{
  "version": 1,
  "include": [
    "/api/*"
  ],
  "exclude": [
    "/index.html",   // Исключаем главную страницу
    "/style.css",    // Исключаем стили
    "/"              // Исключаем корневой путь
  ]
}
